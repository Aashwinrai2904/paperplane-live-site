# Fireworks AI — powers real nomic-embed-text-v1.5 embeddings.
# Leave unset to run on deterministic mock embeddings (no cost, fully testable).
FIREWORKS_API_KEY=

# Supabase — optional persistence of analysis runs.
# Leave unset to run fully in-memory (Light Version default).
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
